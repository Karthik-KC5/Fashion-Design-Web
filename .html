<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Fashion Marketplace - For Gen Z</title>
  <style>
    /* Basic reset and fonts */
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #fff;
      color: #333;
      line-height: 1.4;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      position: relative;
      overflow-x: hidden;
    }
    header {
      background: linear-gradient(90deg, #6f42c1, #a77de9);
      color: white;
      padding: 16px 24px;
      text-align: center;
      font-weight: 700;
      font-size: 1.5rem;
      position: sticky;
      top: 0;
      z-index: 1000;
    }
    nav {
      display: flex;
      justify-content: center;
      gap: 24px;
      margin-top: 12px;
      flex-wrap: wrap;
    }
    nav a {
      color: white;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
      padding: 8px 12px;
      border-radius: 6px;
      transition: background-color 0.3s ease;
      cursor: pointer;
    }
    nav a:hover {
      background-color: #9155e3;
    }
    main {
      flex-grow: 1;
      padding: 24px;
      max-width: 1200px;
      margin: 0 auto;
    }
    /* Hero Section */
    .hero {
      background: #f4edf9;
      border-radius: 14px;
      padding: 48px 24px;
      text-align: center;
      margin-bottom: 32px;
      box-shadow: 0 4px 8px rgba(101, 41, 255, 0.1);
    }
    .hero h1 {
      font-size: 2rem;
      font-weight: 800;
      color: #6f42c1;
      margin-bottom: 12px;
    }
    .hero p {
      font-size: 1.1rem;
      color: #6a6180;
    }
    /* Category Cards */
    .categories {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: 20px;
      margin-bottom: 40px;
    }
    .category-card {
      background: white;
      border: 1px solid #ded5f9;
      border-radius: 14px;
      padding: 24px;
      text-align: center;
      cursor: pointer;
      transition: box-shadow 0.3s ease;
    }
    .category-card:hover {
      box-shadow: 0 8px 16px rgba(111, 66, 193, 0.25);
    }
    .category-card img {
      max-width: 80px;
      margin-bottom: 16px;
      user-select: none;
    }
    .category-card h3 {
      font-weight: 700;
      color: #532d9d;
      font-size: 1.1rem;
    }
    /* Products Grid */
    .products {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px,1fr));
      gap: 24px;
    }
    .product-card {
      background: white;
      border-radius: 14px;
      border: 1px solid #e6e0f7;
      overflow: hidden;
      box-shadow: 0 2px 5px rgba(101, 41, 255, 0.05);
      display: flex;
      flex-direction: column;
      transition: box-shadow 0.25s ease;
    }
    .product-card:hover {
      box-shadow: 0 10px 20px rgba(111, 66, 193, 0.3);
    }
    .product-image {
      width: 100%;
      height: 190px;
      object-fit: cover;
      user-select: none;
    }
    .product-info {
      padding: 12px 16px;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .product-title {
      font-size: 1rem;
      margin-bottom: 8px;
      font-weight: 700;
      color: #4a2baf;
    }
    .product-price {
      font-weight: 700;
      color: #af52de;
      font-size: 1.1rem;
      margin-bottom: 12px;
    }
    .product-btn {
      background: #6f42c1;
      border: none;
      color: white;
      font-weight: 600;
      padding: 10px;
      border-radius: 12px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    .product-btn:hover {
      background: #9155e3;
    }

    /* Wishlist and Cart Icon Styles */
    .icon-btn {
      position: fixed;
      top: 80px;
      right: 20px;
      background: #6f42c1;
      border: none;
      border-radius: 50%;
      width: 44px;
      height: 44px;
      color: white;
      font-size: 20px;
      cursor: pointer;
      box-shadow: 0 4px 6px rgba(111, 66, 193, 0.4);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1100;
    }
    .icon-btn:hover {
      background: #9155e3;
    }
    .icon-btn + .icon-btn {
      margin-top: 12px;
    }

    /* Overlay for wishlist and cart windows */
    .overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background-color: rgba(0,0,0,0.4);
      z-index: 1050;
      display: none;
    }
    .overlay.active {
      display: block;
    }

    /* Wishlist & Cart Side Window */
    .side-window {
      position: fixed;
      top: 0;
      right: 0;
      width: 320px;
      max-width: 90vw;
      height: 100vh;
      background: white;
      padding: 24px;
      box-shadow: -4px 0 12px rgba(111,66,193,0.3);
      z-index: 1100;
      overflow-y: auto;
      transform: translateX(100%);
      transition: transform 0.3s ease;
      display: flex;
      flex-direction: column;
    }
    .side-window.active {
      transform: translateX(0);
    }
    .side-window h2 {
      color: #6f42c1;
      margin-bottom: 24px;
    }
    .side-window .close-btn {
      align-self: flex-end;
      background: none;
      border: none;
      font-size: 24px;
      font-weight: 700;
      color: #6f42c1;
      cursor: pointer;
      margin-bottom: 12px;
    }

    /* Items list */
    .item-list {
      list-style: none;
      flex-grow: 1;
      overflow-y: auto;
      padding-right: 8px;
    }
    .item-list li {
      display: flex;
      align-items: center;
      margin-bottom: 16px;
      border-bottom: 1px solid #e6e0f7;
      padding-bottom: 12px;
    }
    .item-list img {
      width: 60px;
      height: 60px;
      object-fit: cover;
      border-radius: 8px;
      margin-right: 12px;
      user-select: none;
    }
    .item-details {
      flex-grow: 1;
    }
    .item-details h3 {
      font-size: 1rem;
      color: #4a2baf;
      margin-bottom: 6px;
    }
    .item-details .price {
      color: #af52de;
      font-weight: 700;
      font-size: 0.9rem;
    }
    .item-actions {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 6px;
    }
    .item-actions button {
      background: #6f42c1;
      border: none;
      color: white;
      border-radius: 6px;
      padding: 6px 10px;
      font-size: 0.85rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: 90px;
      text-align: center;
    }
    .item-actions button:hover {
      background: #9155e3;
    }
    .item-actions input[type="number"] {
      width: 50px;
      font-size: 1rem;
      text-align: center;
      border: 1px solid #ccc;
      border-radius: 6px;
      padding: 4px;
    }

    /* Empty state */
    .empty-msg {
      text-align: center;
      color: #999;
      font-size: 1.1rem;
      margin-top: 60px;
      flex-grow: 1;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    /* Checkout button in cart */
    .checkout-btn {
      background: #af52de;
      border: none;
      color: white;
      font-weight: 700;
      padding: 14px;
      font-size: 1rem;
      border-radius: 12px;
      margin-top: auto;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: 100%;
    }
    .checkout-btn:hover {
      background: #d38afb;
    }

    /* Responsive adjustments */
    @media (max-width: 640px) {
      nav {
        flex-direction: column;
        gap: 12px;
      }
      .hero {
        padding: 32px 16px;
      }
      .side-window {
        width: 100vw;
        max-width: 100vw;
      }
      .icon-btn {
        width: 50px;
        height: 50px;
        font-size: 22px;
      }
    }
  </style>
</head>
<body>
  <header>
    Fashion Marketplace
    <nav>
      <a href="#trending">Trending</a>
      <a href="#categories">Categories</a>
      <a href="#products">All Products</a>
      <a id="wishlistToggle" tabindex="0">Wishlist ♥</a>
      <a id="cartToggle" tabindex="0">Cart 🛒</a>
      <a href="#profile">Profile</a>
    </nav>
  </header>
  <main>
    <section class="hero" id="trending">
      <h1>Discover the Latest Trends for Gen Z</h1>
      <p>Your go-to marketplace for fashion that speaks your vibe.</p>
    </section>

    <section id="categories" class="categories">
      <div class="category-card" tabindex="0">
        <img src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/33666c3a-3198-4af3-b90b-9288d0543fcd.png" alt="Streetwear category icon" />
        <h3>Streetwear</h3>
      </div>
      <div class="category-card" tabindex="0">
        <img src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/ab38faf1-b080-45f3-9988-b492deef808c.png" alt="Vintage category icon" />
        <h3>Vintage</h3>
      </div>
      <div class="category-card" tabindex="0">
        <img src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/c0083e99-b3ee-480d-a1eb-4af4fa791213.png" alt="Sustainable fashion icon" />
        <h3>Sustainable</h3>
      </div>
      <div class="category-card" tabindex="0">
        <img src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/fa652c95-3ba4-4a6d-930e-e9c7849e1304.png" alt="Accessories category icon" />
        <h3>Accessories</h3>
      </div>
    </section>

    <section id="products" class="products" aria-label="Product listings">
      <article class="product-card" tabindex="0" data-id="1" data-name="Purple Oversized Jacket" data-price="79.90" data-img="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/6ad99d5f-2e3b-4740-b3f2-885b9e8cc6bd.png">
        <img class="product-image" src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/3c223712-2f7e-428f-b577-91ca16f1aae5.png" alt="Cool purple jacket product" />
        <div class="product-info">
          <h2 class="product-title">Purple Oversized Jacket</h2>
          <div class="product-price">$79.90</div>
          <button class="product-btn add-to-cart" type="button" aria-label="Add Purple Oversized Jacket to cart">Add to Cart</button>
          <button class="product-btn add-to-wishlist" style="margin-top:6px; background:#9155e3;" type="button" aria-label="Add Purple Oversized Jacket to wishlist">Add to Wishlist</button>
        </div>
      </article>
      <article class="product-card" tabindex="0" data-id="2" data-name="Vintage Floral Shirt" data-price="49.50" data-img="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/dad4d4f4-a82d-40a5-b8f0-99826241510b.png">
        <img class="product-image" src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/3eb30575-6513-4b11-bda5-f8f8e6195547.png" alt="Vintage patterned shirt product" />
        <div class="product-info">
          <h2 class="product-title">Vintage Floral Shirt</h2>
          <div class="product-price">$49.50</div>
          <button class="product-btn add-to-cart" type="button" aria-label="Add Vintage Floral Shirt to cart">Add to Cart</button>
          <button class="product-btn add-to-wishlist" style="margin-top:6px; background:#9155e3;" type="button" aria-label="Add Vintage Floral Shirt to wishlist">Add to Wishlist</button>
        </div>
      </article>
      <article class="product-card" tabindex="0" data-id="3" data-name="Eco Sneakers" data-price="59.99" data-img="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/7da9a129-732a-4759-94ff-5f0d126845fe.png">
        <img class="product-image" src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/759bfadc-e7a6-467c-b157-f911c8e4a006.png" alt="Eco-friendly sneakers product" />
        <div class="product-info">
          <h2 class="product-title">Eco Sneakers</h2>
          <div class="product-price">$59.99</div>
          <button class="product-btn add-to-cart" type="button" aria-label="Add Eco Sneakers to cart">Add to Cart</button>
          <button class="product-btn add-to-wishlist" style="margin-top:6px; background:#9155e3;" type="button" aria-label="Add Eco Sneakers to wishlist">Add to Wishlist</button>
        </div>
      </article>
      <article class="product-card" tabindex="0" data-id="4" data-name="Neon Beanie Hat" data-price="19.99" data-img="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/ad856b1f-c82b-4243-a9f7-de6185d99159.png">
        <img class="product-image" src="https://storage.googleapis.com/workspace-0f70711f-8b4e-4d94-86f1-2a93ccde5887/image/7f25b9ec-6bd3-43bd-a9ed-2a153d4d972c.png" alt="Bright colorful hat product" />
        <div class="product-info">
          <h2 class="product-title">Neon Beanie Hat</h2>
          <div class="product-price">$19.99</div>
          <button class="product-btn add-to-cart" type="button" aria-label="Add Neon Beanie Hat to cart">Add to Cart</button>
          <button class="product-btn add-to-wishlist" style="margin-top:6px; background:#9155e3;" type="button" aria-label="Add Neon Beanie Hat to wishlist">Add to Wishlist</button>
        </div>
      </article>
    </section>
  </main>

  <!-- Wishlist Overlay -->
  <div id="wishlistOverlay" class="overlay"></div>
  <aside id="wishlistWindow" class="side-window" aria-label="Wishlist">
    <button class="close-btn" aria-label="Close wishlist window">&times;</button>
    <h2>Your Wishlist</h2>
    <ul class="item-list" id="wishlistList">
      <p class="empty-msg" id="wishlistEmptyMsg">Your wishlist is empty.</p>
    </ul>
  </aside>

  <!-- Cart Overlay -->
  <div id="cartOverlay" class="overlay"></div>
  <aside id="cartWindow" class="side-window" aria-label="Shopping cart">
    <button class="close-btn" aria-label="Close cart window">&times;</button>
    <h2>Your Cart</h2>
    <ul class="item-list" id="cartList">
      <p class="empty-msg" id="cartEmptyMsg">Your cart is empty.</p>
    </ul>
    <button class="checkout-btn" id="checkoutBtn" style="display:none;">Checkout</button>
  </aside>

  <!-- Floating icon buttons -->
  <button id="wishlistToggleButton" class="icon-btn" aria-label="Open wishlist">♥</button>
  <button id="cartToggleButton" class="icon-btn" aria-label="Open cart" style="top: 130px;">🛒</button>

  <script>
    // Store wishlist and cart in memory (can persist with localStorage later)
    const wishlist = new Map();
    const cart = new Map();

    // UI references
    const wishlistWindow = document.getElementById('wishlistWindow');
    const wishlistOverlay = document.getElementById('wishlistOverlay');
    const wishlistList = document.getElementById('wishlistList');
    const wishlistEmptyMsg = document.getElementById('wishlistEmptyMsg');

    const cartWindow = document.getElementById('cartWindow');
    const cartOverlay = document.getElementById('cartOverlay');
    const cartList = document.getElementById('cartList');
    const cartEmptyMsg = document.getElementById('cartEmptyMsg');
    const checkoutBtn = document.getElementById('checkoutBtn');

    // Toggle functions
    function toggleWishlist() {
      const isActive = wishlistWindow.classList.contains('active');
      if (isActive) closeWishlist();
      else openWishlist();
    }
    function toggleCart() {
      const isActive = cartWindow.classList.contains('active');
      if (isActive) closeCart();
      else openCart();
    }
    function openWishlist() {
      wishlistWindow.classList.add('active');
      wishlistOverlay.classList.add('active');
      renderWishlist();
    }
    function closeWishlist() {
      wishlistWindow.classList.remove('active');
      wishlistOverlay.classList.remove('active');
    }
    function openCart() {
      cartWindow.classList.add('active');
      cartOverlay.classList.add('active');
      renderCart();
    }
    function closeCart() {
      cartWindow.classList.remove('active');
      cartOverlay.classList.remove('active');
    }

    // Render wishlist items
    function renderWishlist() {
      wishlistList.innerHTML = '';
      if (wishlist.size === 0) {
        wishlistEmptyMsg.style.display = 'flex';
        return;
      }
      wishlistEmptyMsg.style.display = 'none';
      wishlist.forEach(item => {
        const li = document.createElement('li');
        li.innerHTML = `
          <img src="${item.img}" alt="${item.name}" />
          <div class="item-details">
            <h3>${item.name}</h3>
            <div class="price">$${item.price}</div>
          </div>
          <div class="item-actions">
            <button aria-label="Remove ${item.name} from wishlist" data-id="${item.id}" class="remove-wishlist-btn">Remove</button>
            <button aria-label="Add ${item.name} to cart" data-id="${item.id}" class="move-to-cart-btn">Add to Cart</button>
          </div>
        `;
        wishlistList.appendChild(li);
      });
    }

    // Render cart items
    function renderCart() {
      cartList.innerHTML = '';
      if (cart.size === 0) {
        cartEmptyMsg.style.display = 'flex';
        checkoutBtn.style.display = 'none';
        return;
      }
      cartEmptyMsg.style.display = 'none';
      checkoutBtn.style.display = 'block';
      cart.forEach(item => {
        const li = document.createElement('li');
        li.innerHTML = `
          <img src="${item.img}" alt="${item.name}" />
          <div class="item-details">
            <h3>${item.name}</h3>
            <div class="price">$${item.price}</div>
            <label>
              Qty:
              <input type="number" min="1" max="10" value="${item.qty}" data-id="${item.id}" class="cart-qty-input" aria-label="Change quantity for ${item.name}">
            </label>
          </div>
          <div class="item-actions">
            <button aria-label="Remove ${item.name} from cart" data-id="${item.id}" class="remove-cart-btn">Remove</button>
          </div>
        `;
        cartList.appendChild(li);
      });
    }

    // Add product to wishlist
    function addToWishlist(product) {
      if (!wishlist.has(product.id)) {
        wishlist.set(product.id, {...product});
        alert(`${product.name} added to wishlist.`);
      } else {
        alert(`${product.name} is already in your wishlist.`);
      }
      renderWishlist();
    }

    // Add product to cart
    function addToCart(product, qty=1) {
      if (cart.has(product.id)) {
        const existing = cart.get(product.id);
        existing.qty = Math.min(existing.qty + qty, 10);
      } else {
        cart.set(product.id, {...product, qty});
      }
      alert(`${product.name} added to cart.`);
      renderCart();
    }

    // Event delegation for add to cart and wishlist buttons on products
    document.querySelectorAll('.product-card').forEach(card => {
      const id = card.dataset.id;
      const name = card.dataset.name;
      const price = parseFloat(card.dataset.price);
      const img = card.dataset.img;

      const product = {id, name, price, img};

      card.querySelector('.add-to-cart').addEventListener('click', () => {
        addToCart(product);
      });
      card.querySelector('.add-to-wishlist').addEventListener('click', () => {
        addToWishlist(product);
      });
    });

    // Wishlist toggle buttons
    document.getElementById('wishlistToggle').addEventListener('click', toggleWishlist);
    document.getElementById('wishlistToggle').addEventListener('keypress', e => { if(e.key === 'Enter') toggleWishlist(); });
    document.getElementById('wishlistToggleButton').addEventListener('click', toggleWishlist);

    // Cart toggle buttons
    document.getElementById('cartToggle').addEventListener('click', toggleCart);
    document.getElementById('cartToggle').addEventListener('keypress', e => { if(e.key === 'Enter') toggleCart(); });
    document.getElementById('cartToggleButton').addEventListener('click', toggleCart);

    // Close buttons
    wishlistWindow.querySelector('.close-btn').addEventListener('click', closeWishlist);
    wishlistOverlay.addEventListener('click', closeWishlist);

    cartWindow.querySelector('.close-btn').addEventListener('click', closeCart);
    cartOverlay.addEventListener('click', closeCart);

    // Wishlist remove and move to cart buttons delegation
    wishlistList.addEventListener('click', (e) => {
      if (e.target.classList.contains('remove-wishlist-btn')) {
        const id = e.target.dataset.id;
        wishlist.delete(id);
        renderWishlist();
      }
      if (e.target.classList.contains('move-to-cart-btn')) {
        const id = e.target.dataset.id;
        const product = wishlist.get(id);
        if(product) {
          addToCart(product);
          wishlist.delete(id);
          renderWishlist();
        }
      }
    });

    // Cart remove buttons delegation and quantity changes
    cartList.addEventListener('click', (e) => {
      if (e.target.classList.contains('remove-cart-btn')) {
        const id = e.target.dataset.id;
        cart.delete(id);
        renderCart();
      }
    });
    cartList.addEventListener('change', (e) => {
      if(e.target.classList.contains('cart-qty-input')) {
        const id = e.target.dataset.id;
        let value = parseInt(e.target.value);
        if(isNaN(value) || value < 1) value = 1;
        else if(value > 10) value = 10;
        e.target.value = value;
        if(cart.has(id)) {
          const product = cart.get(id);
          product.qty = value;
          cart.set(id, product);
          renderCart(); // re-render to ensure consistency, e.g. totals if implemented
        }
      }
    });

    // Checkout button
    checkoutBtn.addEventListener('click', () => {
      if(cart.size === 0) {
        alert("Your cart is empty.");
        return;
      }
      alert("Thank you for your purchase! (This is a demo alert)");
      cart.clear();
      renderCart();
      closeCart();
    });

  </script>
  <footer>
    &copy; 2024 Fashion Marketplace - Made by a student for Gen Z shoppers
  </footer>
</body>
</html>

